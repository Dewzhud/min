local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()
local Window = OrionLib:MakeWindow({Name = "ZolClient", HidePremium = false, SaveConfig = true, IntroText ="Zol"})

local Tab = Window:MakeTab({
	Name = "client",
	Icon = "",
	PremiumOnly = false
})

Tab:AddToggle({
	Name = "spider",
	Default = false,
	Callback = function(Value)
		_G.Spider = Value
Spider()
	end    
})

-- ตั้งค่าเริ่มต้นของตัวแปร Toggle เป็น false
_G.Spider = false

-- ตรวจสอบเงื่อนไข
if _G.Spider then

    print("Toggle เปิด")
else
    -- ทำงานเมื่อ _G.Toggle เป็น false
    print("Toggle ปิด")
end
